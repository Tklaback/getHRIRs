using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PasteSquares : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Mesh mesh = GameObject.Find("Sphere").GetComponent<MeshFilter>().mesh;
        //Vector3[] normals = mesh.normals;
        //print(normals.Length);
        //for (int i = 0; i < normals.Length; i++)
        //{
        //    GameObject cube = GameObject.CreatePrimitive(PrimitiveType.Cube);
        //    Vector3 newNormal = normals[i] * 9;
        //    cube.transform.position = newNormal;
        //    cube.transform.localScale = new Vector3(2.3f, 2.3f, .01f);
        //    cube.transform.LookAt(new Vector3(0, 0, 0), cube.transform.up);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
